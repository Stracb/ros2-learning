# 00 · 接续说明（新会话必读）

> **这份文档的作用**：把整个项目的背景、环境、进度、教学法**一次性交接**给新会话。
>
> **怎么用**：在新对话窗口里，把这份文档**整份粘贴**给助手，并说一句
> "请先读这份接续说明，然后我们继续"。助手读完就能无缝接手，不需要你重新解释。
>
> **最后更新**：2026-09-21（第 11 天）

---

# 一、用户背景（重要）

| 项 | 内容 |
|---|---|
| **ROS 基础** | **完全从零开始**，此前没学过 |
| **Linux 基础** | **初学者**，会基本操作但不熟 |
| **Python 基础** | **只有一点**（能读懂简单代码，不熟练）|
| **C++ 基础** | **基本不会**（前 10 个阶段都用 Python，暂时不影响）|
| 学习目标 | **机器人仿真 + 求职** |
| 沟通语言 | 中文 |
| 当前进度 | **第 11 天**。环境完成；**11 个节点**跑通；三种通信机制收发两端都会；参数/YAML/launch 已巩固；**能自定义消息类型并在节点里收发（阶段三最后一块已攻克一半）** |

## 讲解方式要求（严格遵守）

1. **涉及 Linux 命令时，逐部分说明它在干什么**
   例：`cp -L a b` 要说清 `cp` / `-L` / `a` / `b` 分别是什么。
   **不要默认用户看得懂 `sed` / `grep` / `for` 循环 / `cut` 等。**

2. **讲 ROS 概念时，先讲"它解决什么问题"，再讲怎么用。**

3. **用户说"不懂"时，换个说法重讲，不要重复原话。**

4. **不要一次抛出大量命令**，分步骤给，每步说清预期结果。

5. **主动解释目的**。用户会问"这些步骤是干嘛的"，甚至主动要求"告诉我还有哪些步骤"。

6. **符号规范（第 9 天确定）**：
   - **不要用星星符号**
   - 其他彩色符号可以用（如勾叉）
   - 强调也可以用纯文字："（重要）"、"（注意）"
   > 用户原话（第一次）："少用有颜色的符号表情，例如星星和感叹标识"
   > 用户原话（第 9 天改口）："我还是想着恢复之前那种有颜色的符号表情，只是不要用星星了"

7. **用户明确表示过**："有时候稍微说多一些、节奏快些我也能接受，不懂的我会问"。
   —— 节奏可以适当快，但**关键概念必须讲透**。

8. **每次要写 launch 文件 / 参数文件时，先问一句"走哪一版"（第 10 天用户明确要求）**：
   - **快速版**：`parameters=['/home/pummy/.../xxx.yaml']`（绝对路径写死）+ setup.py 只注册 launch
   - **正规版**：`get_package_share_directory('包名')` 算出路径 + setup.py 注册 `config/`
   > 用户原话（第 10 天）："请以后每次写的时候都请提醒我是否要进行改造。"
   > 意思是：**不要先教土办法、再让他返工改造；一开始就问清楚，直接按选定版本写。**

9. **新增 launch 文件 / 参数文件后，务必提醒两件事**：
   - 注册进 `setup.py` 的 `data_files`（否则 `ros2 launch` 找不到）
   - **改了 setup.py 就必须重编**（第 7 天规则的第二种情况）

10. **每天早上开始学习前，先主动查一遍宿主机状态（第 11 天用户明确授权）**：
    - **VMware 快照清单**（名字 + 有没有带内存文件）
    - **虚拟机文件夹总大小**
    - 把结果**主动报给用户**，再开始当天内容
    > 用户原话（第 11 天）："**我允许你以后也去看一下快照再回答信息，
    > 倒不如说每天早上开始前，你都可以去检查一下我当前的电脑状态。**"
    >
    > **为什么要这样**：第 11 天助手犯过一次错 —— **只在快照管理器里扫了一眼名字，
    > 就断言"两个快照都没拍内存"，实际各带一个 12.4 GB 的 `.vmem`。**
    > **能查证的，就不要靠印象回答。**
    >
    > **查法见下面「3.2 虚拟机」里的「状态自检」两条命令。**

---

# 二、项目目标

从旧环境迁移到新环境，然后按学习路线学到能求职的水平。

| | 旧环境（已删除）| 新环境（当前）|
|---|---|---|
| 系统 | Ubuntu 22.04.5 | **Ubuntu 24.04.5 LTS** |
| ROS 2 | Humble（2027/5 EOL）| **Jazzy Jalisco**（LTS 至 2029）|
| 仿真 | **无** | **Gazebo Harmonic 8.11.0** |
| 内存 | 4 GB（跑不动仿真）| 12 GB |

**迁移原因**：旧环境只有 4 GB 内存跑不动仿真；Humble 搭配的 Gazebo Classic
已于 2025 年 1 月 EOL；用户当时零沉没成本，切换代价最低。

---

# 三、环境完整状态

## 3.1 宿主机

| 项 | 值 |
|---|---|
| 系统 | Windows 11（build 26200）|
| CPU | Intel Core i7-14650HX（24 逻辑处理器）|
| 内存 | 31.8 GB |
| 磁盘 | C 约 110 GB 可用、D 约 298 GB、E 约 280 GB |
| 虚拟机软件 | VMware Workstation 17.6.3，**装在 `E:\VMware\`** |

## 3.2 虚拟机

| 项 | 值 |
|---|---|
| 位置 | **`E:\Ubuntu-24.04.5\`** |
| 配置文件 | `Ubuntu24.04.5 64 位.vmx` |
| 分配资源 | 12 GB 内存 / 8 vCPU / 40 GB 磁盘 / 3D 加速开启 |
| 文件夹占用 | 约 23 GB |
| **IP 地址** | **192.168.150.129**（VMware NAT 自动分配，可能变；变了用 `hostname -I` 查）|
| SSH | 已装并设为开机自启（`sudo systemctl enable --now ssh`）|
| 用户名 | `pummy` |

**快照（可回滚）：**

| 快照名 | 内容 | 内存文件 |
|---|---|---|
| `12-自定义服务` | 自定义服务试运行 DrawPolygon | ❌ **`Snapshot9.vmem` 12,416 MB** |

> **第 13 天早上自检结果**：第 12 天的两个旧快照（`07-画正方形以及参数化`、`10-参数与launch巩固`）
> **已被删除**，新拍了 `12-自定义服务` —— **回收了约 12.4 GB**。
>
> ⚠️ **但新快照又勾上了"拍摄虚拟机内存"**（`Snapshot9.vmem` 12.4 GB）。
> 想彻底清掉：**先拍一个不带内存的新快照 → 再删 `12-自定义服务`**。

> **权威来源**：直接读 `E:\Ubuntu-24.04.5\Ubuntu24.04.5 64 位.vmsd`（GBK 编码）。
> **快照名里的中文要用 GBK 解码才看得对**（UTF-8 读出来全是乱码）。
>
> **两个快照都拍了内存，合计白占约 24 GB**（虚拟机文件夹共 38.88 GB）。
> 第 11 天曾误判为"都没有拍内存"——**那是只在快照管理器里看了一眼名字，没查文件**。
> 详见 `08-踩坑总集.md` **坑 7**。

> **拍摄新快照时务必取消勾选「拍摄虚拟机内存」**（注意）—— 本机内存 12 GB，
> 勾选后每个快照占约 12 GB；不勾选只要几百 MB。
>
> **待办**：清理这两个内存快照可回收约 24 GB（做法见 `04` 待办 D）。

### 状态自检（每天早上开始前跑这两条）

> 在**宿主机**的 PowerShell 里跑。**能查证的，就不要靠印象回答。**

```powershell
# ① 快照清单（.vmsd 文件是 GBK 编码，必须显式解码，否则中文全是乱码）
$enc = [System.Text.Encoding]::GetEncoding(936)
$t = $enc.GetString([System.IO.File]::ReadAllBytes('E:\Ubuntu-24.04.5\Ubuntu24.04.5 64 位.vmsd'))
[regex]::Matches($t, 'snapshot\d+\.(displayName|description) = "(.*)"') |
  ForEach-Object { $_.Groups[2].Value }

# ② 有没有"拍了内存"的快照 + 文件夹总大小
Get-ChildItem 'E:\Ubuntu-24.04.5' -Force -File |
  Where-Object { $_.Extension -in '.vmem','.vmsn' } |
  ForEach-Object { '{0,-34} {1,10:N0} MB' -f $_.Name, ($_.Length/1MB) }
'{0:N2} GB 总占用' -f ((Get-ChildItem 'E:\Ubuntu-24.04.5' -Force -File |
  Measure-Object Length -Sum).Sum/1GB)
```

**怎么判断**：

| 看到什么 | 说明 |
|---|---|
| 出现 **`.vmem`** 文件（每个约 12,000 MB）| ❌ 那个快照**拍了内存**，纯浪费 |
| 只有 `.vmsn`（每个几 MB）| ✅ 没拍内存 |
| 文件夹 **15 GB 上下** | ✅ 正常（只有虚拟硬盘）|
| 文件夹 **超过 25 GB** | ⚠️ 基本可以断定有内存快照 |

## 3.3 虚拟机内部

| 组件 | 版本 |
|---|---|
| 系统 | Ubuntu 24.04.5 LTS（Noble），内核 7.0.0-31-generic |
| ROS 2 | Jazzy Jalisco |
| 仿真 | Gazebo Harmonic 8.11.0 |
| 图形 | SVGA3D / OpenGL 4.3 硬件加速 |
| VMware Tools | open-vm-tools + open-vm-tools-desktop（均已装）|
| 网络 | NAT，RTT 约 14 ms |

## 3.4 已装工具

`ros-jazzy-desktop`、`ros-jazzy-ros-gz`、`python3-colcon-common-extensions`、
`ros-jazzy-rqt` + 插件、`ros-jazzy-turtlesim`、`ros-jazzy-xacro`、
`ros-jazzy-robot-state-publisher`、`ros-jazzy-joint-state-publisher`、
`ros-jazzy-ros2-control`、`ros-jazzy-ros2-controllers`

## 3.5 关键配置（踩过坑才对的）

| 配置 | 值 | 为什么 |
|---|---|---|
| Ubuntu 源 | 清华 TUNA | 快 |
| ROS 2 源 | **中科大 USTC**（`mirrors.ustc.edu.cn/ros2/ubuntu`，`Types: deb`）| 实测 4.3 MB/s，最快 |
| **Gazebo 渲染引擎** | **ogre**（不是默认的 ogre2）| **VMware SVGA3D 与 ogre2 不兼容，会界面频闪** |
| 渲染引擎配置文件 | `~/.gz/sim/8/gui.config` 和 `server.config` | 两个都改了（后者管传感器渲染）|
| `~/.bashrc` | 已写入 `source /opt/ros/jazzy/setup.bash` 和 `source ~/ros2_ws/install/setup.bash` | 新终端自动加载 |

## 3.6 开发方式

**VS Code Remote-SSH** —— 宿主机 VS Code 连虚拟机的 `192.168.150.129` 编辑代码。

| 优点 | 说明 |
|---|---|
| 代码始终在虚拟机内 | `colcon build` 不会慢 |
| 用 Windows 的编辑器 | 比虚拟机小窗口舒服 |

> **已装扩展**：`Python`（装在 SSH 远程侧）、`Remote - SSH`（本地）。
> **Python 解释器已选** `/usr/bin/python3`。

> **注意**：GUI 程序（turtlesim / RViz / Gazebo）**必须在虚拟机自己的桌面终端运行** ——
> VS Code 的 SSH 终端没有图形显示环境，会报 `could not connect to display`。

---

# 四、工作区状态

## 4.1 目录结构

**权威版本在 `04-学习进度存档.md` 第四节**（完整目录树 + 每个文件的说明）。

> **为什么这里不再抄一份**（重要）：**同一棵树写在两个文件里，必然会不一致。**
> 第 11 天审计时就抓到过 —— `00` 说 10 个节点、`04` 说 11 个；快照记录两边都错。
> **信息只留一份，其它地方放指针。**

一句话概括：

| 位置 | 是什么 |
|---|---|
| `~/ros2_ws/src/my_turtle/` | **ament_python 包** —— 11 个节点 + `config/` + `launch/` |
| `~/ros2_ws/src/my_interfaces/` | **ament_cmake 包** —— 自定义接口（第 11 天新建）|
| `~/ros2_ws/docs/` | 学习文档副本（从宿主机 `E:\ROS2\` 同步）|
| `~/ros2_ws/build/` `install/` `log/` | colcon 自动生成，已在 `.gitignore` 里排除 |

## 4.2 十一个节点一览

| # | 程序 | 类型 | 干什么 | 测试命令 |
|---|---|---|---|---|
| 1 | `draw_circle` | 发布者 + **参数** | 乌龟转圈（第 10 天已参数化）| `ros2 launch my_turtle circle_demo.launch.py`；或 `ros2 run my_turtle draw_circle --ros-args -p linear_speed:=2.0` |
| 2 | `draw_line` | 发布者 | 走直线 | `ros2 run my_turtle draw_line` |
| 3 | `read_pose` | 订阅者 | 打印 x/y | `ros2 run my_turtle read_pose` |
| 4 | `pubsub` | 发布+订阅 | 走到 x>10 停下 | `ros2 run my_turtle pubsub` |
| 5 | `greeter` | 服务端 | `/say_hello` | `ros2 service call /say_hello std_srvs/srv/Trigger` |
| 6 | `turtle_control` | 发布+服务端+定时器 | `/movemove` 控制走停 | `ros2 service call /movemove std_srvs/srv/SetBool "{data: true}"` |
| 7 | `draw_square` | 发布+订阅+状态机+参数 | 画闭合正方形 | `ros2 run my_turtle draw_square --ros-args -p side_length:=3.0` |
| 8 | `move_client` | 服务客户端 | 调 `/movemove` | 先启动 `turtle_control`，再跑它 |
| 9 | `rotate_client` | 动作客户端 | 调 `/turtle1/rotate_absolute` | 先启动 `turtlesim`，再跑它 |
| 10 | `my_rotate_server` | 动作服务端 | 提供 `/my_rotate` | 另开终端 `ros2 action send_goal /my_rotate turtlesim/action/RotateAbsolute "{theta: 1.5708}" --feedback` |
| 11 | `status_bridge` | 订阅 + 发布（**自定义消息**）| 把 `Pose` 翻译成 `TurtleStatus` | 先启动 turtlesim，再 `ros2 run my_turtle status_bridge` + `ros2 topic echo /turtle_status --once` |

> **除了 `greeter` 和 `my_rotate_server`，其他都需要先启动 `turtlesim_node`。**
> **`status_bridge` 是第一个用自定义消息类型的节点**（第 11 天）。

---

# 五、教学法：怎么教这个用户

这部分是项目中最有价值的经验，**新会话请务必照做**。

## 5.1 核心方法：先给结构，再讲细节

**第 3 天的教训**：助手一开始只是"逐行解释代码"，结果让用户做"闭卷重写"时，
用户说"我基本不知道每个类里面有些什么，每个函数是干嘛的" —— **完全写不出来**。

**根因**：用户拿到了一堆零件，但没有图纸。

**现在的做法**：

> **一个节点只有 3 块：**
> | 块 | 名称 | 什么时候跑 | 干什么 |
> |---|---|---|---|
> | 1-A | `__init__` | 创建对象时，一次 | 准备：起名字、建发布者/订阅者/服务、建定时器 |
> | 1-B | 回调函数 | 每次触发，反复 | 干活：造消息、读消息、填 response |
> | 2 | `main()` | 启动时，一次 | 开机 → 造对象 → spin → 清理 |

**讲任何节点前，先把这张"三块图"摆出来。**

## 5.2 新节点用"7 问题推导法"

**不要直接给代码。先让用户回答这 7 个问题：**

| # | 问题 |
|---|---|
| 1 | 节点叫什么名字？ |
| 2 | 要"发"数据吗？发什么？ |
| 3 | 要"收"数据吗？收什么？ |
| 4 | 要"定时"吗？ |
| 5 | 消息/服务/动作类型是什么？ |
| 6 | 要读/填哪些字段？ |
| 7 | 回调里具体干什么？ |

**答完这 7 题，代码自然就出来了。**

**这个方法第 4 天验证有效，之后一直是标准教法。**

## 5.3 卡住时让他翻"清单"，不要翻原代码

- **`05-写节点清单.md`**：只列"该做什么"，不写完整代码 —— 让用户自己回忆语法
- **`06-ROS2命令速查.md`**：命令查询
- **`代码备份/节点模板.py`**、**`launch文件模板.py`**：填空式模板

翻清单是"查语法"，正常；翻原代码是"抄"，效果差。

## 5.4 报错时教他读报错，不要直接给答案

已经教会他识别这些：

| 报错 | 原因 |
|---|---|
| `AttributeError: ... has no attribute 'xxx'` | 方法没缩进到类里 / 名字不一致 / 拼写错误 |
| `UnboundLocalError: ... local variable 'xxx'` | 忘了写 `self.` |
| `Assertion 'PyFloat_Check' failed` + `Aborted` | 浮点字段赋了整数（`2` 应该是 `2.0`）|
| `Package 'xxx' not found` | 终端没 source 工作空间 |
| `No executable found` | 程序名错 / 改了 setup.py 没重编 |
| `could not connect to display` | 在 SSH 终端跑图形程序 |

## 5.5 记忆规律（第 8 天总结出来的，很重要）

对用户做过多次测验，结论非常稳定：

| 内容类型 | 用户表现 | 原因 |
|---|---|---|
| **逻辑 / 原理**（状态机、通信方向、判据）| ✅ **几乎全对** | 理解过就记住 |
| **语法 / 结构**（方法名、文件格式）| ⚠️ **写 1 遍就忘；写 2 遍以上才记住** | 靠重复次数 |
| 环境细节（版本号、配置值）| ❌ 忘 | 不重要，可查 |

**已验证的例子**：

| 内容 | 写过几遍 | 结果 |
|---|---|---|
| 服务端 | 2 遍（`greeter` + `turtle_control`）| ✅ 记得 |
| 状态机 | 3 遍 | ✅ 几乎全对 |
| 客户端 | 1 遍 | ❌ 忘 |
| 参数 | 1 遍 | ❌ 忘 |
| launch | 1 遍 | ❌ 忘 |

> **给新会话的提醒**：
> - **不要强求用户默写只写过 1 遍的东西**
> - **语法类靠"后面用到时自然重复"，不必硬背**
> - 用户自己说过："查表倒是可以知道" —— 这个态度是对的

## 5.6 复习节奏（用户认可）

| 频率 | 做什么 | 时长 |
|---|---|---|
| 每次开场 | 填表复习上次的关键内容 | 5 分钟 |
| 每 3–4 天 | 闭卷重写一个旧节点（挑写过的）| 20 分钟 |
| 每 7 天 | 小整合练习 | 40 分钟 |

**复习用"填表 / 复述 / 闭卷重写"（主动回忆），不要"重读笔记"（被动）。**

---

# 六、已完成的工作

> **逐日明细见 `04-学习进度存档.md` 第八节「会话记录」** ——
> 那里有每天做了什么、产出什么文档、踩了什么教训。
> **这里只留一句话级别的总览**（同样是"不做双份维护"）。

**第 1 天（09-11）· 环境搭建**
诊断旧环境 → 迁移决策 → 建虚拟机 → 装 Ubuntu 24.04.5 → 配镜像源 →
装 ROS 2 Jazzy → 装 Gazebo Harmonic（解决频闪）→ 清理回收约 50 GB

**第 2–11 天（09-12 至 09-21）· 概念与编程**
从"节点、话题到底是什么"一路写到 **11 个节点 + 1 个自定义接口包**，
覆盖**话题 / 服务 / 动作**三种通信机制的收发两端；
参数化、YAML、launch（含正规做法）、自定义 msg 全部跑通。

---

# 七、学习进度与下一步

> **权威版本在 `04-学习进度存档.md`：**
>
> | 想知道什么 | 看 `04` 的哪一节 |
> |---|---|
> | **路线进度（10 个阶段到哪了）** | 第一节 |
> | **下一任务 / 待办 / 知识盲区** | 第三节、第五节 |
> | **每次会话开始/结束的检查清单** | 第九节 |
>
> **新会话读 `04` 就够了，这里不再重复一张表。**

**一句话现状（第 11 天结束）**：
阶段 0 / 1 / 2 已完成，**阶段 3 只差自定义 `srv`**（自定义 `msg` 已打通）。

**第 12 天的约定任务**（用户第 11 天明确说"留到明天"）：
做 `srv/DrawPolygon.srv` + 服务端，把 `draw_square` 升级成"**画任意正多边形**"。

---

# 八、Git 管理

> **权威版本在 `04-学习进度存档.md` 第九、十节** ——
> 那里有「每天收尾的检查清单」和「git 四条命令 + 每次要做什么」。
> **这里只留一张概览表。**

| 项 | 值 |
|---|---|
| 仓库位置 | **`~/ros2_ws`**（工作空间级别）|
| 远程 | GitHub（用户自己的账号）|
| 认证 | **SSH**（`ssh -T git@github.com` 已测试通过）|
| `.gitignore` | `build/` `install/` `log/` `__pycache__/` `*.pyc` `.vscode/` |
| 文档 | 放在 `~/ros2_ws/docs/`（从宿主机 `E:\ROS2\` 同步过来）|

> **重要的架构说明**：
> **文档的"主副本"在宿主机 `E:\ROS2\`** —— 因为助手只能访问宿主机的文件系统。
> **虚拟机里的 `docs/` 是副本**，每次提交前需要同步一次。

---

# 九、已知的坑

> ## 完整坑表见 **`08-踩坑总集.md`（67 条）**
>
> 每条都有 **现象 → 原因 → 解决 → 教训**，末尾还有「**复发过的坑**」统计。
> **遇到报错先搜那里。**
>
> **这里已经删掉了原来的 16 条速查表** —— 它是 `08` 的真子集，而且更旧。
> 只挑三条最要命的放在下面。

| 坑 | 真相 |
|---|---|
| **Gazebo 界面频闪** | Ogre2 与 VMware SVGA3D **不兼容**（**与 3D 加速是否开启无关**），换 `ogre` 引擎 |
| **`--symlink-install` 报 `Is a directory`** | 接口包（ament_cmake + rosidl）和它**天生冲突**。**编 `my_turtle` 加；编 `my_interfaces` 不加** |
| **改了 `setup.py` 不重编 → `No executable found`** | 新增文件 / 改 `setup.py` **必须重编**；另外还要检查 `entry_points` 有没有注册、终端有没有 `source` |

---

# 十、文档索引

**主副本在宿主机 `E:\ROS2\`；虚拟机的 `~/ros2_ws/docs/` 是同步副本。**

| 文件 | 内容 |
|---|---|
| **`04-学习进度存档.md`** | **学习主线存档 —— 每次会话开始/结束都要看和更新** |
| `00-接续说明.md` | 本文件（用户背景、环境、教学法、全部上下文）|
| `01-环境搭建.md` | 从零搭建的完整步骤（已走完，留作参考）|
| `02-学习路线.md` | 14 周学习路线，含每阶段验收标准和求职准备 |
| `03-Linux命令速查.md` | Linux 命令逐项拆解、快捷键、ROS 常用组合 |
| `05-写节点清单.md` | **写节点时的对照清单**：8 步流程 + **20 个形状模板** + 命名规范 + 常见错误 |
| `06-ROS2命令速查.md` | **ROS 2 命令速查**：项目全流程 + 查看类命令 + 报错对照 |
| `07-draw_square代码解析.md` | 闭环控制程序的完整结构解析（状态机 + 测量）|
| **`08-踩坑总集.md`** | **踩过的所有坑（67 条）** —— 现象 / 原因 / 解决 / 教训 + 复发统计。**遇到问题先搜这里** |
| `README.md` | 环境状态、验收记录、踩坑记录 |
| `代码备份/节点模板.py` | 节点骨架（填空式）|
| `代码备份/launch文件模板.py` | launch 文件骨架（填空式）|
| `代码备份/draw_circle_v1.py` | 第一个节点的原版（留作对比）|

## 新会话怎么开始

1. **让助手读 `04-学习进度存档.md`**（看进度和下一步）
2. **再读本文件**（看用户背景、环境、教学法）
3. **开场先做 5 分钟复习**（填表测验上次的内容）
4. **然后按 `04` 第五节的候选任务推进**

## 工作分工（用户安排）

| 窗口 | 用途 |
|---|---|
| **主窗口** | **ROS 2 学习主线的推进** |
| 另一个窗口 | 问专业名词、零散知识（不推进主线）|

> 所以主窗口要负责维护 `04-学习进度存档.md` 的更新。

---

# 十一、关于求职方向（用户明确的目标）

用户学习 ROS 2 是为了**做仿真 + 求职**。学习路线已按此设计：

- 重点放在**仿真栈**（Gazebo → URDF/Xacro → ros2_control → SLAM → Nav2）
- 每阶段都有**验收标准**，避免"看懂了但做不出来"
- 最终产出**一个完整项目 + GitHub 仓库 + 演示视频**作为求职材料
- `02-学习路线.md` 里有**高频面试题清单**和目标企业方向

**目前已经积累的求职材料**：
- GitHub 仓库（代码 + 学习文档）
- **11 个可运行的 ROS 2 节点** + **1 个自定义接口包**（`my_interfaces`）
- 连续 11 天的提交记录

> **提醒求职相关的建议**：ROS 2 是基础技能，真正拿到 offer 的是在某个方向
> （Nav2 调参 / 多机调度 / 视觉跟随）上的深度。**做深一个项目胜过做浅五个。**

---

# 十二、给新助手的最后几句话

1. **这是一个"学习者"项目，不是"交付产品"项目。**
   用户的目标是**学会**，不是尽快跑通。**慢一点、讲清楚，比快更有价值。**

2. **用户的进步是真实且显著的**：
   第 3 天"完全不知道类里有什么" → 第 9 天独立写出动作客户端和服务端。
   **但用户自己会因为"记不住语法"而焦虑** —— 这时要提醒他：
   **理解过的不忘，语法忘了可以查。**

3. **不要替他写代码。** 用 7 问题推导、清单对照、读报错 —— 让他自己写出来。

4. **每次会话结束都要**：
   - 更新 `04-学习进度存档.md`
   - 走一遍 Git 环节（第 8 天起用户的要求）

5. **有疑问先查 `04` 和本文件**，再问用户。
