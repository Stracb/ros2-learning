# 04 · 学习进度存档

> **用途**：学习主线的存档点。**每次会话结束时更新这份文件。**
> 新会话（或新窗口）开始时，先读这份 + `00-接续说明.md`，就能无缝接上。
>
> **最后更新**：2026-09-17（第 7 天）

---

## 一、路线总览与当前进度

| 阶段 | 内容 | 计划周 | 状态 |
|---|---|---|---|
| 0 | 环境搭建 | 第 0 周 | ✅ **完成** |
| **1** | **核心概念与 CLI** | **第 1 周** | ✅ **话题 + 服务已打通**（动作/参数/bag 待补）|
| **2** | **rclpy 编程基础** | 第 2–3 周 | 🔄 **已写 5 个节点（发布者/订阅者/服务端）** |
| 3 | 自定义接口 / Launch / 参数 | 第 4 周 | ⬜ 未开始 |
| 4 | URDF / Xacro 建模 | 第 5 周 | ⬜ 未开始 |
| 5 | Gazebo Harmonic 仿真 | 第 6–7 周 | ⬜ 未开始 |
| 6 | TF2 与 RViz2 | 第 8 周 | ⬜ 未开始 |
| 7 | 传感器仿真与 ros2_control | 第 9 周 | ⬜ 未开始 |
| 8 | SLAM 建图 | 第 10 周 | ⬜ 未开始 |
| 9 | Nav2 自主导航 | 第 11–12 周 | ⬜ 未开始 |
| 10 | 综合项目 | 第 13–14 周 | ⬜ 未开始 |

> **注**：用户主动要求"直接开始写代码"，所以**阶段一和阶段二在并行推进**。
> 阶段一的剩余部分（动作、参数、bag）需要在写代码过程中回头补齐。

> ⚠️ **第 3 天的重要教训（务必看）**：
> 让用户做"闭卷重写 `draw_circle.py`"，**用户完全写不出来**，反馈是
> "基本不知道每个类里面有些什么，每个函数是干嘛的"。
>
> **根因不是记忆力问题，是助手之前只"逐行解释代码"，没给整体结构图。**
> 用户拿到一堆零件却没有图纸。
>
> **已采取的补救**：给了「一个节点只有 3 块」的结构图
> （`__init__` 配置块 / 回调函数干活块 / `main` 启动块）+ 调用时序图。
> 并把练习难度从「闭卷」降级为「半开卷（看结构图填空）」。
>
> **给助手的提醒**：讲代码时**必须先给结构，再讲细节**。不要一上来逐行解释。

> ✅ **第 4 天结果**：补救有效。用户现在能独立写出节点，
> 且已掌握发布者 / 订阅者 / 服务端三种形状。
>
> **新的教法共识（继续沿用）**：
> 1. 新形状 → 先给「形状模板」（放进 `05-写节点清单.md`）
> 2. 让用户先用 7 问题**推导**，再动手写
> 3. 卡住优先翻**清单**，而不是翻原代码
> 4. 报错时教用户**读报错**，不要直接给答案

---

## 二、环境（✅ 已完成，无需重做）

| 项 | 值 |
|---|---|
| 虚拟机位置 | `E:\Ubuntu-24.04.5\`（宿主机） |
| 系统 | Ubuntu 24.04.5 LTS |
| ROS 2 | Jazzy Jalisco |
| 仿真 | Gazebo Harmonic 8.11.0（渲染引擎 ogre） |
| 资源 | 12 GB 内存 / 8 vCPU / 40 GB 磁盘 |
| **IP 地址** | **192.168.150.129**（NAT，可能变动，用 `hostname -I` 查） |
| 开发方式 | VS Code Remote-SSH（已配好，Python 扩展已装到远程，解释器选 `/usr/bin/python3`） |
| 快照 | `03-仿真就绪`（唯一回滚点） |

> 完整环境细节见 `README.md`；搭建步骤见 `01-环境搭建.md`；踩坑记录见 `00-接续说明.md`。

---

## 三、阶段一详细进度

### ✅ 已掌握

| 内容 | 掌握程度 |
|---|---|
| **节点 (Node)** 概念 | ✅ 理解"节点 = 能通信的独立小程序" |
| `ros2 run` / `node list` / `node info` | ✅ 用过 |
| **话题 (Topic)** 概念、发布/订阅模型 | ✅ 理解 |
| `ros2 topic list` / `list -t` / `echo` / `info` | ✅ 用过 |
| `ros2 interface show` 看消息结构 | ✅ 用过 |
| `ros2 topic pub` 手动发消息 | ✅ 用过 |
| `rqt_graph` 看节点拓扑 | ✅ 看过并能读懂 |
| `rqt_publisher` 图形化发话题 | ✅ 试过 |
| **包 (Package)** 结构、`msg`/`srv`/`action` 目录 | ✅ 理解三层结构和"编译时/运行时"两层 |
| **命名空间** 概念（`/turtle1/pose` 的 `turtle1`） | ✅ 理解，并生成过 turtle2 验证 |
| 编译→安装→source→运行的流程 | 🟡 有印象，未亲手走完整流程 |

### ⬜ 阶段一剩余（待补）

| 内容 | 说明 |
|---|---|
| **服务 (Service)** | 只用过 `/spawn` 和 `/clear`，未系统学 |
| **动作 (Action)** | 完全没接触 |
| **参数 (Parameter)** | 完全没接触 |
| `ros2 bag` 录制与回放 | 完全没接触 |
| **QoS** | 完全没接触 |
| 话题特点的对比（Topic vs Service vs Action）| 只讲了 Topic |

> **补课时机建议**：写完第一个节点后，回头补"服务"。因为服务是"一问一答"，
> 和刚学的发布/订阅正好形成对比，理解效果最好。

---

## 四、工作区当前状态

### 已创建

```
~/ros2_ws/                              ← 工作空间
└── src/
    └── my_turtle/                      ← 第一个包
        ├── my_turtle/
        │   ├── __init__.py
        │   ├── draw_circle.py          ← ① 发布者：转圈
        │   ├── draw_line.py            ← ② 发布者：走直线
        │   ├── read_pose.py            ← ③ 订阅者：打印位置
        │   ├── wall_stopper.py         ← ④ 发布+订阅：到边界停下（闭环）
        │   └── greeter.py              ← ⑤ 服务端：提供 /say_hello
        │   └── turtle_control.py       ← ⑥ 发布+服务端+定时器：/movemove 控制走停
        ├── package.xml
        ├── resource/my_turtle
        ├── setup.cfg
        ├── setup.py                    ← entry_points 已注册 6 个程序
        └── test/
```

### 状态：✅ 6 个节点全部跑通

| # | 程序 | 类型 | 学到的形状 |
|---|---|---|---|
| 1 | `draw_circle` | 发布者 | 形状 1-5、7 |
| 2 | `draw_line` | 发布者 | 巩固 |
| 3 | `read_pose` | 订阅者 | **形状 3（订阅）、6（读消息）** |
| 4 | `wall_stopper` | 发布 + 订阅 | **闭环控制**，不需要定时器 |
| 5 | `greeter` | **服务端** | **形状 8（建服务）、9（服务回调）** |
| 6 | `turtle_control` | **发布 + 服务端 + 定时器** | **状态变量 `self.moving`，两个回调协作** |

**用户已掌握：发布 / 订阅 / 服务端 + 状态变量 —— 能写"决策层 + 执行层"的完整结构。**

> ⚠️ **GUI 程序（turtlesim / RViz / Gazebo）必须在虚拟机自己的桌面终端运行** ——
> VS Code 的 SSH 终端没有图形显示环境。纯计算节点没这个限制。

---

## 五、⭐ 下一步（明天从这里开始）

### 🎯 当前状态（更新于第 7 天）

**环境能力已经覆盖：**
| 能力 | 状态 |
|---|---|
| 发布者 / 订阅者 / 服务端 | ✅ 都会写 |
| 定时器 / 状态变量 / 状态机 | ✅ |
| 闭环控制（读传感器→决策→发指令） | ✅ `draw_square` |
| 参数（`declare` / `get` / 命令行 / YAML）| ✅ 用过一遍，**语法会忘，可查** |
| launch 文件（多节点启动 + 传参） | ✅ 第 7 天学会 |

**第 7 天验证：用户能闭卷重写 `read_pose`（只错 1 个 import）。**

### 🎯 明天的建议安排

| 顺序 | 内容 | 时长 |
|---|---|---|
| 1 | **热身复习**（填表，5 分钟）—— 重点测**参数语法**（第 7 天忘了）和 **launch 文件结构** | 5 分钟 |
| 2 | 选一个主任务（见下方待办）| 40–60 分钟 |

### 待办 A：可选 —— 复习（如果用户状态好）

第 5 天已复习过三种节点形状，发现了 3 处模糊（已纠正）：
1. 话题是**单向**、服务是**双向**（用户之前答反）
2. 类比：话题=广播电台，服务=打电话
3. `create_service`（不是 `create_server`）

**第 6 天用户这三道判断题全对 —— 已巩固。**

| | 发布者 | 订阅者 | 服务端 |
|---|---|---|---|
| 建什么 | `create_publisher` | `create_subscription` | `create_service` |
| 谁触发回调 | 定时器 | 消息到达 | 被调用 |
| 回调参数 | `(self)` | `(self, msg)` | `(self, request, response)` |
| 要定时器吗 | ✅ | ❌ | ❌ |
| 要 return 吗 | ❌ | ❌ | ✅ `return response` |

### 待办 B：自由练习（用户自己选）—— 更新于第 7 天

| 选项 | 内容 | 难度 | 状态 |
|---|---|---|---|
| **B1** | 用服务控制乌龟走/停（服务端 + 发布者）| 中 | ✅ **第 5 天完成**（`turtle_control`，服务 `/movemove`）|
| **B2** | **画精确正方形**（发布者 + 订阅者 + **状态机**）| 中高 | ✅ **第 6 天完成**（`draw_square`，首尾闭合 ✅）|
| **B3** | 闭卷重写旧节点 | 易 | ✅ **第 7 天完成**（重写 `read_pose`，只错 1 个 import）|
| **B4** | launch 文件（多节点启动 + 传参）| 中 | ✅ **第 7 天完成** |
| **B5** | **写一个服务客户端**（用代码调用服务）| 中 | ⬜ **推荐下一个** |
| **B6** | **动作 (Action)** | 中 | ⬜ 三大机制最后一个 |
| **B7** | **比例控制优化** `draw_square` | 中 | ⬜ 让它又快又准 |

### 待办 C：补阶段一剩余内容

| 内容 | 状态 |
|---|---|
| 话题 (Topic) | ✅ |
| 服务 (Service) | ✅ 概念 + 服务端 |
| **服务客户端** | ⬜ **服务只写了一半**（只有服务端，没写客户端）|
| **动作 (Action)** | ⬜ 三大机制最后一个 |
| 参数 (Parameter) | ✅ 概念 + 代码（语法会忘，可查）|
| **launch 文件** | ✅ 第 7 天 |
| `ros2 bag` | ⬜ 待学 |
| QoS | ⬜ 待学 |

### 待办 D：收尾

- [x] 快照 `05-服务端完成`（第 5 天）
- [x] 快照 `07-画正方形以及参数化`（第 6 天）
- [ ] 快照 `08-launch文件`（第 7 天，可选）
- [ ] **考虑把代码用 git 管理**（求职需要，一直没做）
- [ ] 清理旧快照（`04-第一个节点集` 和 `05-服务端完成` 内容已被覆盖）

### 📌 常备参考：一个节点只有 3 块

**（这是第 3 天补的核心结构图，反复会用到，保留在这里）**

| 块 | 名称 | 什么时候跑 | 干什么 |
|---|---|---|---|
| **1-A** | `__init__` | 创建对象时，**一次** | **准备**：起名字、建发布者/订阅者/服务、建定时器 |
| **1-B** | 回调函数 | 每次触发，**反复** | **干活**：造消息、填内容、发出去 / 读消息 / 填 response |
| **2** | `main()` | 启动时，**一次** | 开机 → 造对象 → spin → 清理 |

**调用时序：**

```
main() → rclpy.init() → node = XxxNode() → 【__init__ 执行：准备】
      → rclpy.spin(node) → 定时器响 / 消息到达 / 服务被调用 → 调回调 → 循环
```

**类比：开店**
- `__init__` = 开店准备（挂招牌、装设备、设闹钟）
- 回调函数 = 营业接待（有客人/有订单就处理一次）
- `main()` = 开店流程（开机→造店→运转→打烊）

### 📌 常备参考：形状模板在哪儿

| 需要什么 | 去哪查 |
|---|---|
| 写节点的 8 步流程 | `05-写节点清单.md` 第一部分 |
| 建发布者/订阅者/定时器/服务端的写法 | 同上 **第二部分 · 形状 1-9** |
| 命名规范、四个名字的关系 | 同上 **第五部分** |
| 常用 ROS 2 命令 | `06-ROS2命令速查.md` |
| 常见报错对照 | 同上 **第九部分** |

### 📌 待补的阶段一内容（更新于第 4 天）

| 内容 | 状态 |
|---|---|
| 话题 (Topic) | ✅ 概念 + 代码都会 |
| **服务 (Service)** | ✅ 概念 + 代码都会（`greeter`）|
| **动作 (Action)** | ⬜ 待学 —— 三大机制最后一个 |
| **参数 (Parameter)** | ⬜ 待学（正好回答"能否不改代码改参数"）|
| `ros2 bag` | ⬜ 待学（数据录制回放）|
| launch 文件 | ⬜ 待学（一次启动多个节点）|
| QoS | ⬜ 待学（第 5 周后）|

---

## 六、关于参数化的重要认知（用户问过）

**当前状态**：`draw_circle.py` 里的参数是**硬编码**的 —— 改行为必须改代码。

```python
msg.linear.x = 2.0      # 硬编码
msg.angular.z = 1.0     # 硬编码
```

**未来的正确做法**：ROS 2 **参数机制** —— 参数放外部 YAML 文件，
运行时用 `-p 参数名:=值` 修改，**不用动代码**：

```python
self.declare_parameter('linear_speed', 2.0)
msg.linear.x = self.get_parameter('linear_speed').value
```

```bash
ros2 run my_turtle draw_circle --ros-args -p linear_speed:=1.0
```

**这是第 4 周的内容**，现在知道有这个方向即可。

---

## 七、用户学习特征（给助手的提醒）

| 项 | 内容 |
|---|---|
| **Python 基础** | ⚠️ **只有一点**（不是零，但很浅） |
| **C++ 基础** | ❌ **基本不会** |
| **Linux 基础** | ⚠️ **初学者** |
| 学习目标 | 机器人仿真 + 求职 |
| 语言 | 中文 |

### 讲解要求（重要）

1. **每条 Linux 命令都要逐部分解释**（命令名/选项/参数分别是什么）—— 用户明确要求过
2. **讲 ROS 概念先讲"解决什么问题"，再讲怎么用**
3. **Python 语法（类、继承、方法、定时器）需要解释**，不能默认用户懂
4. 用户会主动问"这些是干嘛的"—— 主动解释目的
5. 不要一次抛太多命令，**分步骤给，每步说清预期结果**
6. 用户说"不懂"时换说法重讲

### 已产出的辅助文档

| 文件 | 用途 |
|---|---|
| `03-Linux命令速查.md` | Linux 命令逐项拆解 + 快捷键 + ROS 常用组合 |
| `02-学习路线.md` | 14 周路线，每阶段有验收标准 + 求职准备 |

---

## 七、会话记录

### 第 1 天（2026-09-11）：环境搭建

诊断旧环境 → 决定迁移 → 建虚拟机 → 装 Ubuntu 24.04.5 → 配镜像源 →
装 ROS 2 Jazzy → 装 Gazebo Harmonic（解决频闪）→ 清理回收 50 GB

**产出**：`README.md`、`01-环境搭建.md`、`02-学习路线.md`、`00-接续说明.md`

### 第 2 天（2026-09-12）：概念学习 + 准备写代码

| 完成的事 |
|---|
| 阶段一：节点概念 + `ros2 node` 系列命令 |
| 阶段一：话题概念 + `ros2 topic` 系列命令（list/echo/info/interface show/pub）|
| 阶段一：用命令行让乌龟画正方形（画歪了 —— **这正是写代码的动机**）|
| 弄清 `/turtle1/pose` 的命名空间机制（生成 turtle2 验证）|
| 讲清"包 → 接口定义 → 编译 → 节点 → 话题"的完整层级和依赖关系 |
| 讲清 `ros2 pkg create` 生成的骨架、哪些要手动改 |
| 讲清"编译→安装→source→运行"的 6 步流程 |
| 配置 VS Code Remote-SSH（IP 192.168.150.129）|
| 创建 `~/ros2_ws/src/my_turtle` 包 |
| **写并跑通第一个 ROS 2 节点 `draw_circle.py`** —— 乌龟自己转圈 ✅ |
| 改 `setup.py` 注册程序入口、`colcon build`、`source` 加载环境 |
| **产出**：`03-Linux命令速查.md`、本文件、`代码备份/draw_circle_v1.py` |

**用户主动提出的重要问题**：感觉"能读懂代码但写不出"，担心学习效果。
**已达成约定**：逐步减少给完整代码的比例 ——
`draw_circle`（完整）→ `draw_square`（骨架+填空）→ 订阅者（需求+提示）→ 独立写。

**下一步**：见第五节（闭卷重写 + 参数实验 + 写订阅者）

### 第 3 天（2026-09-13）：厘清概念 → 撞上结构缺口

**✅ 完成的事**

| 完成的事 |
|---|
| 厘清用户提问的三个概念问题（colcon 扫描机制、内外层命名、包 vs 节点）|
| 弄明白 `colcon build` 不认识 "src"，靠 `COLCON_IGNORE` 跳过 build/install/log |
| 弄清内外层命名**可以不一样**（外层=ROS 包名，内层=Python 模块名），但建议一致 |
| 澄清"包跑起来变成节点"不精确 —— **是包里的可执行程序**跑起来变成节点 |
| 用"对讲机"类比打通了用户最大的混淆点：**话题名（频道）vs 消息类型（语言）** |
| 精确纠正了 `source` 机制：source **执行** setup，setup 把路径**写进环境变量**；`ros2 run` 读环境变量找包 |
| 用户答对全部验证题 → **概念部分基本通过** |

**❌ 撞上的问题（重要）**

让用户"闭卷重写 `draw_circle.py`"，**用户完全写不出来**，反馈：
> "我基本不知道每个类里面有些什么，每个函数是干嘛的"
>
> 用户表示情绪受挫（"我有点死了"）。

**根因**：助手之前是**逐行解释代码**，从没给过**整体结构图**。
用户拿到了零件却没有图纸。

**补救措施**：给出「一个节点只有 3 块」的结构图 + 调用时序图 + 开店类比，
并把练习从「闭卷」降级为「半开卷填空」。

**用户当天到此暂停**，要求明天再重写。

### 第 4 天（2026-09-14）：爆发式推进 —— 写了 5 个节点

**✅ 完成的事**

| 时间 | 完成 |
|---|---|
| 开场 | 三个结构问题**全答对**（`__init__` vs 回调、定时器如何记住函数名、`main()` 五行）|
| 上午 | 半开卷填空重写 `draw_circle`，写出骨架；修 3 个 bug（缩进 / 变量名不一致 / `destroy.node`）后跑通 |
| 上午 | 自己定位并修复 `1` vs `1.0`（浮点字段赋整数导致 C 层断言失败）|
| 中午 | 写 `draw_line`（发布者，独立写）|
| 中午 | 写 `read_pose`（**订阅者**，独立写）；打通"`msg` 参数从哪来"、"`self.x` 怎么存" |
| 晚上 | 写 `wall_stopper`（**发布 + 订阅闭环**）；理解"订阅者回调自带主循环，不需要定时器" |
| 晚上 | 学 **服务概念**（和话题对比、`srv` 的 `---` 分隔、`ros2 service call` 实操）|
| 晚上 | 写 `greeter`（**服务端**）；修 `self.operation` 漏写 `self.` 和缩进两个 bug |

**📚 当天产出的工具文档**

| 文件 | 内容 |
|---|---|
| `05-写节点清单.md` | 8 步流程 + **9 个形状模板** + 命名规范 + 常见错误 + 何时重编 + 快速查找索引 |
| `06-ROS2命令速查.md` | ROS 2 命令分类速查 + 三种相似命令对照 + 报错对照表 |

**🔍 发现并解决的问题**

| # | 问题 | 解法 |
|---|---|---|
| 1 | 用户反映"记不住结构、不知道写什么" | 建「写节点清单」（只列步骤不写代码）+ 教「7 问题推导法」 |
| 2 | 用户反映"清单里找不到东西，只能翻原代码" | 清单加**快速查找索引** |
| 3 | 用户问"节点命名规则" —— 清单确实缺 | 清单加**第五部分：命名规范** |
| 4 | 用户写服务端时清单缺服务形状 | 清单加**形状 8、9** |

**💡 教法经验（继续沿用）**

1. **新形状 → 先给形状模板，再让用户推导**，不要直接给完整代码
2. **报错时教用户读报错**（`UnboundLocalError` 说明少写了 `self.`、`AttributeError: no attribute` 多半是缩进），不要直接给答案
3. **用户查类型是正常的**，不要要求背类型
4. **练习用「7 问题推导」**：节点名 / 发什么 / 收什么 / 定时吗 / 什么类型 / 填什么字段 / 回调干什么 —— 答完代码就出来了

**下一步**：见第五节（复习三种形状 → 自由练习 B1「用服务控制乌龟走/停」）

### 第 5 天（2026-09-15）：复习 + 写出最复杂的节点

**✅ 完成的事**

| 完成的事 |
|---|
| **热身复习**三种节点形状（填表），找出 3 处模糊并纠正 |
| 纠正 1：**话题是单向、服务是双向**（用户此前答反）|
| 纠正 2：类比 —— 话题=广播电台，服务=打电话（用户此前"不清楚"）|
| 纠正 3：**`create_service`**（不是 `create_server`）|
| 学会**怎么查有哪些服务类型**：`ros2 interface list \| grep "/srv/"` |
| 讲清 **`request` / `response` 是什么**、为什么能 `.success`（srv 文件的字段定义 → 编译生成类 → 运行时创建实例）|
| 讲清 `msg` / `request` / `response` 是**同一回事**（ROS 2 按类型定义创建好递给你）|
| **新概念：状态变量** —— 用 `self.moving` 让两个回调互相"对话" |
| 讲清"决策层（服务）+ 执行层（定时器）"是真实机器人的标准模式（Nav2 同构）|
| 写出第 6 个节点 `turtle_control`：**发布者 + 服务端 + 定时器 + 状态变量** |
| 自己踩并解决 3 个 bug：服务名写成类型、`messsage` 拼写错、忘写 `return response` |
| **自己给服务起名 `movemove`** —— 证明理解了"服务名是自己定的" |

**📚 文档更新**

| 文件 | 更新内容 |
|---|---|
| `06-ROS2命令速查.md` | 新增**「📌 零、名字记不住？查这里」**（名字对照、`create_xxx` 参数顺序表、类型字段表、import 速查）；服务一节扩写为完整实战流程；报错表拆成"命令类 / Python 类 / 无报错但行为不对"三张；节点清单更新到 6 个 |

**⚠️ 用户反馈的困难（重要）**

> **"话题名字和服务名字记不住，`create` 里面的参数名字记不住"**

**已应对**：速查表新增「零、名字记不住？查这里」。
**助手提醒**：名字是查的不是背的，不要施压；持续强化"**带引号=名字，不带引号=类型**"。

**下一步**：见第五节

### 第 6 天（2026-09-16）：画正方形 + 参数化

**✅ 完成的事**

| 完成的事 |
|---|
| 热身复习三种节点形状 —— 3 道判断题全对（第 5 天的纠正都记住了）|
| **写出第 7 个节点 `draw_square`** —— 分三步做的：<br>① 走一条边（状态机 + 距离测量）<br>② 加转弯（角度测量）<br>③ 循环 4 次（计数器 + 回到 forward 时更新起点）|
| 学会 **`math.hypot`** 算距离、**`abs(角度差)`** 算转角 |
| 讲清 **`msg.theta` 在 ±π 回绕** 的坑（turtlesim 会把 theta 归一化）|
| **遇到"转过冲 6°"** —— 用户自己改角速度试出"角速度小 → 过冲小"，**首尾成功闭合** ✅ |
| 讲清 **累积误差** = 4 个角各差几度累积起来 —— 这是真实机器人学的核心问题 |
| **学参数（Parameter）**：<br>① `ros2 param list/get/set` 改 turtlesim 背景色体验<br>② 用 `declare_parameter` + `get_parameter().value` 改造 `draw_square`（3 个参数）<br>③ 写 YAML 参数文件 + `--params-file` 加载<br>④ 把 `config/` 注册进 `setup.py` 的 `data_files` |
| 讲清参数的运行原理：**命令行 → rclpy 记录 → `declare` 时二选一（命令行覆盖默认）→ `get` 取出** |

**📚 文档产出**

| 文件 | 内容 |
|---|---|
| **`07-draw_square代码解析.md`**（新建）| 10 个部分：整体结构、`__init__`、`operation` 五段、状态机详解、两个测量、贯穿规律、运行时序、通用性、可改进点、踩过的坑 |
| `06-ROS2命令速查.md`（大幅扩充）| **补上 `ros2 pkg create`**；新增 **「🔗 项目全流程」大章节**（从建工作空间到运行调试的 9 步 + 可复制清单）；参数一节扩写为运行时操作/启动传参/YAML/代码写法；节点清单更新到 7 个 |

**💡 用户反馈**

> **"这个参数部分感觉代码量有点大"**

**已应对**：拆解了参数的运行原理（命令行 → declare 二选一 → get 取出），
并强调**只有 6 行、每个节点都这么写、熟练后不觉得多**。

> 用户也反馈：**"有时候稍微说多一些节奏快些我也能接受，不懂的我会问"**
> —— 可以适当加快节奏，不必过度拆解每个小步骤。

**下一步**：见第五节

### 第 7 天（2026-09-17）：闭卷重写 + launch 文件

> 用户白天全是课，晚上才继续。安排了两个较轻的任务。

**✅ 完成的事**

| # | 内容 |
|---|---|
| **热身复习** | 状态机、两个测量**全对**；参数语法（`declare`/`get`/`--ros-args`）**忘了** —— 但用户自己说"查表可以知道" |
| **A. 闭卷重写 `read_pose`** | ✅ **从空文件独立写出并跑通**，全程只错 1 处：`from geometry.msg` 应为 `from turtlesim.msg` |
| 顺带讲清 | **`geometry_msgs/msg/Pose` 和 `turtlesim/msg/Pose` 是两个不同的类型**（前者是 3D 位姿含四元数，后者是 2D 的 x/y/theta）|
| **B. launch 文件** | ✅ 写 `launch/square_demo.launch.py`，一条命令启动 turtlesim + draw_square；`setup.py` 注册 `launch/` 到 `data_files` |
| 学会形状 | **形状 12：launch 文件骨架**（`generate_launch_description`）<br>**形状 13：`Node(...)` 动作**（package / executable / name / output / parameters）|
| **进阶** | 在 launch 里用 `parameters=[{'side_length': 3.0, ...}]` 传参，**成功** |
| **踩坑** | 在 VS Code SSH 终端跑 `ros2 launch` → `turtlesim_node` 报 `could not connect to display` 崩溃 → **改用虚拟机桌面终端解决** |
| **更正了一个理解** | `--symlink-install` 下，改**已注册文件的内容**（launch/config 也算）**不用重编**；只有**新增文件**才要重编 |

**📊 重要观察：用户"忘了什么"很有规律**

| 类型 | 表现 | 结论 |
|---|---|---|
| **概念/原理**（状态机、测量）| ✅ 记住 | **亲手写过三轮 → 记住了** |
| **语法/命令**（参数相关）| ❌ 忘了 | **只用过一次 → 忘了，但可查** |

> **给助手的提醒**：这印证了"用会了 vs 背会了"的区别。
> **概念类要确保用户亲手写过；语法类不必强求记住，只要会查。**

**📚 文档更新**

| 文件 | 更新 |
|---|---|
| `06-ROS2命令速查.md` | 两处"何时重编"表**更正为精确规则**（已注册文件改内容不用重编；新增文件要重编），并记录了第 7 天的实测验证 |

**下一步**：见第五节

---

## 八、每次都更新的检查清单

会话开始时：

- [ ] 虚拟机开机了吗？
- [ ] VS Code 连上 `192.168.150.129` 了吗？
- [ ] 读一遍第五节的"下一步"

会话结束时：

- [ ] 更新第一节的进度状态
- [ ] 更新第五节"下一步"
- [ ] 在第七节追加本次会话记录
- [ ] 更新"最后更新"日期
- [ ] ⭐ **走一遍 git 环节**（用户第 8 天明确要求）：

```
cd ~/ros2_ws
git status
git add .
git commit -m "第N天：xxx"
git push
```

> **git 环节要做什么：**
> 1. **把 `E:\ROS2\*.md` 同步进 `~/ros2_ws/docs/`**（文档在宿主机，仓库在虚拟机）
> 2. 提醒用户执行上面的四条命令
> 3. **提交说明的格式**：`第N天：主要做了什么`
> 4. 如果用户贴出报错，帮 TA 看

---

## 九、Git 管理（第 8 天建立）

| 项 | 值 |
|---|---|
| 仓库位置 | `~/ros2_ws`（工作空间级别）|
| 远程 | GitHub（用户自己的账号）|
| 认证 | SSH（`ssh -T git@github.com` 已测试通过）|
| `.gitignore` | `build/` `install/` `log/` `__pycache__/` `*.pyc` `.vscode/` |
| 文档 | 放在 `~/ros2_ws/docs/`（从宿主机 `E:\ROS2\` 同步过来）|

> ⚠️ **文档的"主副本"在宿主机 `E:\ROS2\`** —— 因为助手只能访问宿主机的文件系统。
> **虚拟机里的 `docs/` 是副本**，每次提交前需要同步一次。
